import App from "../App";

// Uppercase of Header.js's H is naming convention
const Header = () => {
    return (
    <header>
    <h1>Hello Header</h1>
    </header>
    );
    };
    export default Header;